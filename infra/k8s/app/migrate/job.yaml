apiVersion: batch/v1
kind: Job
metadata:
  name: postgres-migrate
  namespace: app
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: sgawalsh/redis-queue-migrate:latest
          envFrom:
            - configMapRef:
                name: api-config
            - configMapRef:
                name: postgres-config
            - secretRef:
                name: postgres-secret