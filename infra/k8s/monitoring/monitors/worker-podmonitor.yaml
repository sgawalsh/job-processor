apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: worker-podmonitor
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - app
  selector:
    matchLabels:
      app: worker
  podMetricsEndpoints:
    - port: metrics
      interval: 5s